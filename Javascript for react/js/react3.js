//templete literals

const student ={
    name:'gaurav',
    gurd:'happy'
};
const teacher = {
    name:'yub',
    room:'l55'
};
let message = `${student.name} please listin ${teacher.name} and ${teacher.room}`;
console.log(message);